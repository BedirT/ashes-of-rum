# Agent Instructions

Before planning, designing, or implementing this game, read the repository-root
[`DESIGN.md`](DESIGN.md) in full. It is the canonical approved product and prototype
contract.

- Preserve the locked decisions and explicit deferrals in `DESIGN.md` unless the user
  explicitly changes them.
- Treat numeric balance values identified as tunable as implementation parameters, not
  product commitments.
- Do not silently expand the rapid prototype into vertical-slice or production scope.
- Keep `DESIGN.md` current when the user approves a design change.
- At the time this contract was recorded, implementation was intentionally deferred. A
  later explicit request to build or implement authorizes work under this contract.
